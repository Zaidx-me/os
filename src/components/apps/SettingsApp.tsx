"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Settings (settings) — content lands in todo 33. */
export function SettingsApp() {
  return <AppPlaceholder appId="settings" title="Settings — placeholder" note="content lands in todo 33" />;
}

export default SettingsApp;
