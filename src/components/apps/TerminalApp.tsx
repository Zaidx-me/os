"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Terminal (terminal) — shell lands in todo 24-25. */
export function TerminalApp() {
  return <AppPlaceholder appId="terminal" title="Terminal — placeholder" note="shell lands in todo 24-25" />;
}

export default TerminalApp;
