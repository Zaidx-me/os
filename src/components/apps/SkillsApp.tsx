"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Skills (skills) — content lands in todo 19. */
export function SkillsApp() {
  return <AppPlaceholder appId="skills" title="Skills — placeholder" note="content lands in todo 19" />;
}

export default SkillsApp;
