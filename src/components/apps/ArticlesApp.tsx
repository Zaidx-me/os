"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Articles (articles) — content lands in todo 23. */
export function ArticlesApp() {
  return <AppPlaceholder appId="articles" title="Articles — placeholder" note="content lands in todo 23" />;
}

export default ArticlesApp;
