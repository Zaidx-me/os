"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Projects (projects) — content lands in todo 18. */
export function ProjectsApp() {
  return <AppPlaceholder appId="projects" title="Projects — placeholder" note="content lands in todo 18" />;
}

export default ProjectsApp;
