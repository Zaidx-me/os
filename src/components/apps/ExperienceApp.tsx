"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Experience (experience) — content lands in todo 20. */
export function ExperienceApp() {
  return <AppPlaceholder appId="experience" title="Experience — placeholder" note="content lands in todo 20" />;
}

export default ExperienceApp;
