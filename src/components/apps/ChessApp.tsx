"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Chess (chess) — content lands in todo 28. */
export function ChessApp() {
  return <AppPlaceholder appId="chess" title="Chess — placeholder" note="content lands in todo 28" />;
}

export default ChessApp;
