"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Contact (contact) — content lands in todo 22. */
export function ContactApp() {
  return <AppPlaceholder appId="contact" title="Contact — placeholder" note="content lands in todo 22" />;
}

export default ContactApp;
