"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** Resume (resume) — content lands in todo 21. */
export function ResumeApp() {
  return <AppPlaceholder appId="resume" title="Resume — placeholder" note="content lands in todo 21" />;
}

export default ResumeApp;
