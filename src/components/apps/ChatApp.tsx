"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** ZaidGPT (chat) — content lands in todo 31. */
export function ChatApp() {
  return <AppPlaceholder appId="chat" title="ZaidGPT — placeholder" note="content lands in todo 31" />;
}

export default ChatApp;
