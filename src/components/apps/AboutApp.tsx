"use client";

import { AppPlaceholder } from "@/components/apps/AppPlaceholder";

/** About (about) — content lands in todo 17. */
export function AboutApp() {
  return <AppPlaceholder appId="about" title="About — placeholder" note="content lands in todo 17" />;
}

export default AboutApp;
