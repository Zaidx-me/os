# /api/contact (todo 22) — Resend email relay. Read server-side only.
# The real contact email is PII: keep it in CONTACT_TO_EMAIL, never in
# code or data files.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
# Optional verified-sender override (default: ZaidOS <onboarding@resend.dev>).
RESEND_FROM=
