## Task 20 (Experience & Education app) — 2026-08-11
- NO-DATA CATEGORY RULE: plan mentioned a "Side projects (whatbot, applicator)" timeline category, but experience.ts has no such entry. Do NOT synthesize it — render exactly the data-layer groups (education/work/freelance). Acceptance only required >=3 entries incl. BSIT + Tech Bridge + education labeled; the data satisfies it.
- PERIOD CONTRACT: `period` is undefined for every entry; `current` is the flag. Render `entry.period ?? (entry.current ? "Current" : "—")`. The QA failure scenario ("unknown date renders '—' not 'undefined'") is locked by a DOM-wide `not.toContain("undefined")` assert + an exact-text assert on tech-bridge's period.
- GROUPED TIMELINE SHAPE: group by `type` in a fixed order (education/work/freelance), section header = icon + label (graduation-cap/briefcase/globe); entries are hairline cards with role, org (only when present — the freelance entry has NO org and asserting that absence is the "no invented org" QA), period, bullets.
- Suite now: unit 223 (22 files), e2e 136, tsc/lint/build clean.

## Task 19 (Skills app) — 2026-08-11
- C++ IS A RESERVED-PARSE KEY: object keys like `C++:` in a Record<...> literal are a HARD parse error under vite:oxc ("Expected `,` or `}` but found `++`"). MUST be quoted: `"C++": "code"`. Same gotcha applies to any key containing `+`, `.` is fine unquoted but quote it too for consistency. Verify by running the targeted vitest spec (transform fails before any test runs, so a clean targeted run is the check).
- ICON GLYPH ADDITIONS (Icon.tsx): adding a glyph = lucide import + ICONS map entry; the IconName union auto-derives, so no type edits elsewhere. Verified all names against the INSTALLED lucide-react 1.31.0 via `require('lucide-react')` before importing (brand icons are removed in 1.x, but utility icons like Server/Smartphone/Container/Palette/Zap/Atom/Workflow/Layers/LayoutDashboard/Puzzle/Package all exist).
- SKILLS UI SHAPE: group sections `data-testid="skills-group-<id>"`; chips `skills-chip-<slugified-name>`; stack section `skills-stack` + `skills-stack-bar-<slug>`; disclaimer `skills-disclaimer`. Note text rendered as `— <note>` inside each chip (visible at all widths; the plan wants icon+name+note per chip, don't hide the note behind a breakpoint).
- VIBES-NOT-METRICS: decorative level bars get explicit static widths + `motion-safe:animate-pulse` (respects reduced motion via Tailwind's motion-safe variant; no infinite rAF). The disclaimer sentence is ALWAYS rendered whenever bars exist — plan requires it.
- Suite now: unit 219 (21 files), e2e 132, tsc/lint/build clean.

## Task 18 (Projects app) — 2026-08-11
- CARD GRID TESTID PITFALL: the status badge uses `projects-card-<id>-status`, which ALSO matches `/^projects-card-/` in getByTestId — the unit test counted 24 (12 cards + 12 badges). Query cards with a tighter regex `/^projects-card-[a-z0-9]+(?:-[a-z0-9]+)*$/` + filter out `-status` suffix, OR give badges a non-card prefix (`projects-badge-<id>-status`). For e2e use `locator("[data-testid^='projects-card-']")` only where badges can't interfere (they share the prefix too — count assertions must subtract or use a distinct prefix).
- FILTER-ACTIVE VISUAL STATE: active tab uses `border-zaid-accent bg-zaid-accent/10 text-zaid-accent`; inactive `text-zaid-muted hover:...`. Detail pane renders INSIDE the same scrollable root (below the grid) rather than a separate panel — keeps mobile/desktop simple and avoids a second scroll container.
- REL NOOPENER CONTRACT: every external link (Live demo / Repo / Article / Design / More on GitHub) gets `target="_blank" rel="noopener noreferrer"` — asserted in unit + e2e. Article links are internal routes (e.g. /articles/building-whatsapp-gateway) rendered as regular <a> with the same new-tab contract.
- ICON WRAPPER IS THE ONLY ICON PATH (src/components/ui/Icon.tsx): lucide-react 1.x removed brand icons; all app icons go through the typed <Icon name={IconName}> map. Adding a glyph = add the lucide import + a `key: value` in ICONS (IconName union derives automatically). `archive` was added for the Archived badge — no Icon wrapper tests exist, so additions are unblocked.
- SORTING FEATURED FIRST: `[...list].sort((a,b) => Number(b.featured) - Number(a.featured))` — stable sort keeps original order within featured/non-featured groups (projects.ts is already featured-first, so the sort is a belt-and-suspenders guarantee, unit-asserted).
- Suite now: unit 215 (20 files), e2e 128, tsc/lint/build clean.

## Task 17 (About app) — 2026-08-11
- PROFILE PHOTO PROCUREMENT: `curl -A "<browser-like UA>" https://zaidx.me/assets/Profile-YD56AsA9.jpg` returns HTTP 200 `content-type: image/jpeg` (77,507 bytes, Cloudflare edge cache HIT, immutable cache-control) — hotlink protection was NOT an issue from a curl UA, but the plan mandated a browser UA + verify the bytes anyway. `file`/`xxd` are NOT installed on this box; verify a JPEG via `head -c 3` (expect `ff d8 ff`) and parse dimensions from the SOF0 marker with node (914x1145) — confirmed a real image, not an HTML/403 page. Store at `public/images/profile.jpg` (public/ dir now exists; `next/image` serves local /public assets with no remotePatterns config).
- NEXT/IMAGE ACCEPTANCE TRAP: the plan acceptance is literally "image has src=/images/profile.jpg" — but next/image's default loader rewrites local src to `/_next/image?url=...&w=...&q=75`, so the DOM src would FAIL a `/images/profile\.jpg/` match... it actually matches because the encoded url param still contains `/images/profile.jpg`? NO — the url is percent-encoded (`%2Fimages%2Fprofile.jpg`), so `\/images\/profile` does NOT match the encoded string. Fix: pass `unoptimized` to the <Image> so the DOM src stays EXACTLY `/images/profile.jpg` (fine for a small static local photo). If optimizing were required, assert on the `/_next/image?url=` prefix instead. Plan: "image has src=/images/profile.jpg" → `unoptimized`.
- ABOUT COPY COMES 100% FROM THE DATA LAYER (site.ts): name/owner, roleLine, bio (2 paragraphs in his voice), personalityChips — nothing hardcoded in AboutApp.tsx except the repo-count fact "29" (published GitHub fact, not derived from shipped data). Quick stats derive projects.length (12) + articles.length (4). The old-template meta description ("product designer working on web & mobile apps") is asserted ABSENT in both unit + e2e.
- TESTID SCHEME for content-driven apps: keep the outer `data-testid="app-content-<appId>"` (WindowHost.test.tsx + apps.spec.ts + app window e2e all assert it) — the real component REPLACES AppPlaceholder's body but must preserve that testid on its root. Inner section testids: `about-name`, `about-role`, `about-photo`, `about-chip-<slugified>`, `about-stat-<name>`. Chips testid slugify: `chip.replace(/[^a-z0-9]+/gi, "-").toLowerCase()`.
- e2e `not.toContainText` is substring-based — for the QA failure test asserting "no template copy", assert BOTH the specific old meta-description phrase AND a canary word ("uses") absent. New About spec adds 4 tests to the suite (122 total = 118 + 4, 61/project x2).
- Suite now: unit 209 (19 files), e2e 122, tsc/lint/build clean — a task's gate is the FULL suite, not the new spec only.

## Task 15 (app registry + window content provider) — 2026-08-11
- APP REGISTRY (src/lib/apps.tsx): `AppMeta = { id, title, icon: ReactNode, keywords: string[], component: LazyExoticComponent<WindowAppComponent>, defaultSize: {w,h}, defaultWorkspace?: WorkspaceId }`. All 11 apps use `React.lazy(() => import("@/components/apps/<Name>App"))` — the host (WindowHost.tsx) imports ONLY `getAppMeta` + the `WindowAppComponent` type, never a concrete app (plan MUST NOT: no static app imports in the host). `getAppMeta(id)` returns AppMeta | undefined; `appTitle(id)` falls back to humanized id. Registry order mirrors APP_IDS.
- REACT 19 LAZY IS AN OBJECT: `React.lazy()` in 19.2.8 returns `{ $$typeof: Symbol.for("react.lazy"), _payload, _init }` — NOT a function. Assert `app.component.$$typeof === Symbol.for("react.lazy")`; `typeof app.component === "function"` FAILS (first unit run: 1 failed, 191 after fix).
- WINDOW HOST CONTRACT (src/components/wm/WindowHost.tsx): `({ windowId, appId })` -> `getAppMeta(appId)`; UNKNOWN appId throws `Unknown app id "<id>" — no entry in APPS (src/lib/apps.tsx)` (rendered by the nearest error boundary → red frame, QA failure scenario). Renders the lazy component wrapped in `<Suspense fallback={<div data-testid="window-app-loading">...` and passes `{ windowId, close, minimize, maximize, setTitle }`. Window.tsx content area = `<WindowHost windowId={current.id} appId={current.appId} />` inside data-testid="window-content".
- App modules are simple default-export client components rendering a scrollable placeholder `<div data-testid="app-content-<appId>">` (AppPlaceholder.tsx shared frame) with a "lands in todo N" note — real features arrive in todo 17+. Every app e2e asserts BOTH `window-<appId>` (frame) AND `app-content-<appId>` (lazy body actually mounted).
- setTitle pattern (src/store/wm.ts): additive `setTitle(id, title)` guarded `set((s) => { const win = s.windows[id]; if (win === undefined) return s; return { windows: { ...s.windows, [id]: { ...win, title } } }; })`.
- openApp stays PERMISSIVE (registry-driven but fallback-safe): `getAppMeta` supplies defaultSize/defaultWorkspace (explicit ws param wins)/defaultTitle, but unknown ids still open with `defaultTitle(id)` + DEFAULT_WINDOW_SIZE — wm.test.ts opens "files" and must keep passing. Strictness lives in WindowHost (throws), NOT the store.
- LAUNCHER E2E TESTIDS (re-learned, cost 24 failures): there is NO `launcher-results` testid. Real ones (Launcher.tsx): `launcher-backdrop` (187), `launcher` (193), `launcher-input` (204), `launcher-grid` (219, empty query), `launcher-empty` (236, no matches), `launcher-list` (243, results), and per-row `launcher-result-${entryId}` (282, entryId = app.id for apps, command.id for commands). Deterministic open: `launcher-input`.fill(appId) then click `launcher-result-${appId}` (proven in launcher.spec.ts: fill + Enter also works).
- FULL E2E SUITE NOW = 104 tests (52/project x2): task-14 baseline 80 + 12 apps.spec (6/project) + 6 launcher.spec... 104 = 80 baseline + 24 apps.spec (12 x2). All-green run is 3.3m with the dev server already on :3000 (Playwright-spawned npm run dev exits 127 with no node on PATH).
- Gate ordering lesson: run the FULL suite, not just the new spec — the initial targeted run of apps.spec+launcher.spec passed, but that masked nothing; the full run surfaced all 24 apps.spec failures at the same locator. Targeted run first (fast iteration on the new spec), then full suite for the release gate.

## Task 14 (edge-snap + keyboard tiling) — 2026-08-11
- SNAP CONTRACT (src/store/wm.ts): `SNAP_EDGE_ZONE = 40`; `SnapDir = "left"|"right"|"full"`; pure `snapBounds(vp, dir)` — left `{x:8, y:48, w:vw/2-12, h:vh-56}` (x = MAXIMIZE_GAP, y = WAYBAR_H+8, w = 50%-12px per plan), right `{x:vw/2+4, w:vw/2-12}` (symmetric gap), full = `maximizeBounds(vp)`; `snapZoneAt(vp, point)` — left/right when x<40 / x>vw-40, full when y<40 (drag-to-top now SNAPS FULL, overriding task 13's top clamp). `WindowState.floatBounds: FloatBounds|null` (null on open). `WmStore.snap(id, dir)` sets mode=tile + bounds and records floatBounds ONLY while `mode==="float"` — re-snapping a tiled window must NOT overwrite the original float rect. `toggleFloat(id)` restores floatBounds if present, else dragBounds-center (a fully-tiled window can still restore).
- HOTKEY REMAP (src/lib/hotkeys.ts): arrows left KEY_TO_ACTION and are resolved by a new `arrowAction(event)` BEFORE the lookup — plain `Mod+Left/Right` = tile, `Mod+Up` = tile full, `Shift+Mod+Left/Right` = moveToWorkspace, `Mod+F` = toggleFloat. The Shift modifier test runs on BOTH platforms (Mac-style keymap has no Meta; each keymap builds full actions).
- ORCHESTRATOR (src/lib/wm/actions.ts): `snapWindow(id, dir)` = `wm.snap` + `focusWindow` (snap must raise — the store's snap doesn't touch z). Page wiring calls `useWmStore.getState().toggleFloat(id)` for Mod+F (NOT the old setMode flip).
- SNAP PREVIEW: a `createPortal(..., document.body)` div `data-testid="snap-preview"`, fixed z-30, `border-2 border-zaid-accent/60` + backdrop blur. CRITICAL: set explicit CSS props `left/top/width/height` — React IGNORES a `{x,y,w,h}` keyed style object (caught by a failing toHaveStyle test).
- DRAG-SNAP FLOW (Window.tsx): pointerdown saves origin; window-level pointermove computes new bounds via dragBounds AND `snapZoneAt(getViewport(), next point)` to set/clear snapZone state (preview follows the pointer zone); pointerup, if a zone is set, calls snapWindow (sets mode=tile) else just clears the zone — the preview disappears either way. Window root carries `data-mode={current.mode}` so e2e can assert tile vs float.
- E2E DETERMINISM: desktop icon grid is `flex-col flex-wrap content-start` (top-left), so a left-snapped window COVERS the next app's icon — the side-by-side test snaps chess RIGHT first, then opens terminal (its icon is top-left... no — desktop icons are on the LEFT, so opening a NEW app is blocked while chess covers the left; order matters: chess-right → open terminal → terminal-left). Snap e2e asserts exact rects (left snap at 1440x900 = x 8, y 48, w 708) with ±2px tolerance AND asserts `data-mode="tile"`. e2e must reuse the already-running dev server (Playwright-spawned `npm run dev` exits 127 with no node on PATH).
- FULL-E2E ENV (this sandbox): `PATH=/tmp/node-v22.17.1-linux-arm64/bin`, `LD_LIBRARY_PATH=/tmp/opencode/pwlibs/l:/tmp/opencode/deps/usr/lib/aarch64-linux-gnu:/tmp/opencode/pwlibs/root/usr/lib/aarch64-linux-gnu`, `FONTCONFIG_FILE=/tmp/opencode/fc/fonts.conf` (includes deps/etc/fonts/conf.d — the older /tmp/opencode/fonts.conf lacks it). Full suite = 80 tests (40/project x2), 3.3m.

## Task 13 (window chrome) — 2026-08-11
- WINDOW CONTRACT (src/components/wm/Window.tsx): `motion.div role="dialog"` with `data-testid="window-<appId>"` (matches appId, NOT the win-<n> store id), `data-window={current.id}`, data-focused/minimized/maximized attrs, aria-hidden when minimized. Rendered absolutely inside WorkspaceView's layer from the wm store (left/top/width/height/zIndex). Titlebar = AppIcon + truncate title + 3 square controls (lucide Minus/Square/Copy/X). Structure: glass frame (h-full flex-col) with titlebar `h-9 shrink-0` + content `flex-1`; resize-handle overlay is a SEPARATE sibling `absolute inset-0` layer rendered AFTER the frame.
- CRITICAL pointer-events lesson: the 8-handle overlay MUST be `pointer-events-none` with each handle `pointer-events-auto`, or the inset-0 layer intercepts EVERY titlebar click/drag (16/18 window e2e tests failed; only SE-resize passed). Handles use negative offsets (-top-1.5 etc.) so they stick out ~6px past the window edge — standard WM hit area.
- GEOMETRY HELPERS (pure, in src/store/wm.ts, all tested): `MAXIMIZE_GAP = 8`, `maximizeBounds(vp)` = {x:8, y:40+8, w:vw-16, h:vh-40-16}, `dragBounds(start, dx, dy, vp)` clamps x∈[0,vw-w] and y∈[WAYBAR_H, vh-h] (dragging is clamped INSIDE the viewport-minus-waybar box, so the titlebar never slides under the bar — the "drag beyond top clamps at waybar height" QA scenario asserts box.y === 40), `resizeBounds(start, dir, dx, dy, vp)` anchors each edge and enforces the 360x240 minimums (re-uses clampWindowBounds math; a 200x200 viewport caps the minimums to vw-16/vh-40-16 like todo 12).
- MAXIMIZE IS RENDER-ONLY: when `maximized`, the component renders `maximizeBounds(getViewport())` and LEAVES the store's float x/y/w/h untouched — restoring is lossless. That's why maximize-then-drag no-ops: startGesture() early-returns `if (current.maximized)`, so no gesture is ever started (the e2e "restore first" test asserts the box doesn't move).
- FOCUS ORCHESTRATOR (src/lib/wm/actions.ts): `focusWindow(id)` refuses cross-workspace focus (getWindowWs !== activeWs), then `workspaces.setFocused(id)` (new action — focus without membership change) + `wm.focus(id)`. Pointerdown on the window root calls focusWindow so a click on the content area or titlebar raises + focuses. TITLEBAR DRAG EXCLUSION: `onTitlebarPointerDown` bails when `(e.target as HTMLElement).closest("button")` — the control buttons never start a drag, and the minimize/maximize/close clicks still work.
- GESTURE MODEL: pointerdown on titlebar/handle sets a `{kind, start:{x,y,w,h}, origin}` gesture in useState; a useEffect adds WINDOW-LEVEL pointermove/pointerup/pointercancel listeners (survive leaving the element; `setPointerCapture` is a best-effort try/catch since jsdom lacks it). pointermove computes dx/dy from the ORIGIN (not the last event) and calls `useWmStore.getState().setBounds(windowId, next)` — the store is the single source of truth, no local element transforms.
- AnimatePresence EXIT via SNAPSHOT: the stores drop the window synchronously on close, but AnimatePresence keeps the child mounted to play the exit animation. Pattern: `const [snapshot, setSnapshot] = useState<WindowState|undefined>(win)` + during-render adjust `if (win !== undefined && win !== snapshot) setSnapshot(win)` (React's derived-state idiom) — when `win` goes undefined, the frame renders the LAST KNOWN state so the exit animates the real geometry. `current = win ?? snapshot`; return null when both undefined.
- E2E RACE (pre-existing, launcher.spec.ts — FIXED): page.tsx attaches the Mod+Space hotkey listener in a useEffect gated on `booted`, which runs AFTER the desktop paints. `bootToDesktop` resolves as soon as the desktop is visible, so a one-shot `Control+Alt+Space` can land before the listener attaches → launcher never opens → fill() times out. Fix: `openLauncher(page)` helper = `expect.poll(async () => { if (await launcher.isVisible()) return true; await press("Control+Alt+Space"); return launcher.isVisible(); }, { timeout: 5000 })`. Poll checks visible FIRST (no toggle-closure oscillation) and only presses while closed; a genuinely broken hotkey still fails the poll.
- E2E geometry determinism: same pattern as waybar.spec — `test.use({ viewport: { width: 1440, height: 900 } })` per describe so hardcoded maximize bounds (1424x844) hold in BOTH projects. openWindow() helper waits for `opacity: 1` so boundingBox isn't measured mid open-animation.
- New window e2e testid: `getByTestId("window-terminal")` etc. — the OLD placeholder testid `ws-window` is gone (desktop.spec + launcher.spec selector updates land in this task's diff).

## Task 8 (waybar top bar) — 2026-08-10
- WAYBAR CONTRACT (src/components/wm/Waybar.tsx — the top bar itself): `<header data-testid="waybar">` fixed inset-x-0 top-0 z-40, `h-[var(--waybar-h)]` (40px), `window-glass` (bg surface 72% + backdrop blur 16px) + border-b hairline, font-mono, root `hidden md:flex` — CSS-only mobile hiding (SSR-safe; e2e forces viewport per describe to make both projects deterministic). Store access: `useWorkspacesStore(selectActiveWs)` for the active pill; click handlers use `useWorkspacesStore.getState().setActive(ws)` (no subscription needed for actions). Launcher button dispatches `window.dispatchEvent(new CustomEvent("zaidos:toggle-launcher"))` — todo 11's Launcher listens for it; NO hotkey wiring here (task 11 owns initHotkeys; a second attach is a no-op anyway). Power menu: Reboot/Log out call the boot store's new `resetBoot` action (src/store/boot.ts — ONE additive `resetBoot: () => set({ booted: false })`, completeBoot + persist partialize untouched); flipping booted=false makes page.tsx's AnimatePresence swap desktop -> BootScreen, so reboot needs NO event plumbing. Log out = 400ms bg-zaid-bg fade overlay (motion.div) then resetBoot; reduced-motion path calls resetBoot directly (no overlay). Menu closes on ESC (window keydown listener added/removed per powerOpen) + click-away backdrop (fixed inset-0 z-40 INSIDE the header's stacking context — z-50 menu still wins because the header is the stacking context root).
- react-hooks v6 NEW RULE `react-hooks/purity` (eslint-config-next 16.3): eager `Date.now()` passed to useRef during render is an ERROR ("Cannot call impure function during render"). Lazy `useState(() => ...)` initializers are NOT flagged (React caches the result). Fix used: uptime counts ticks (`setUptimeSec((s) => s + 1)` in the 1s interval) instead of Date.now() diff — simpler and purity-safe. Clock/date refresh: `setInterval(() => setNow(new Date()), 60_000)` — new Date() inside an interval callback is fine (async-callback exemption).
- CANVAS COLORS CANNOT READ CSS CUSTOM PROPS: `ctx.strokeStyle = "var(--color-zaid-accent)"` silently fails. MatrixRain's readTokens pattern (getComputedStyle with token-mirroring hex fallbacks) is the codebase answer — the sparkline uses it for accent/muted. Also: a 64x12 canvas (class h-3 w-16) has clientWidth 0 in jsdom — guard `w === 0 || h === 0` before sizing (same as MatrixRain fit()).
- LUCIDE 1.x BRAND ICONS CONFIRMED ABSENT: `require('lucide-react').Github` / `.Linkedin` are `undefined`; `Command`/`Power`/`AtSign`/`Link` exist. GitHub/LinkedIn links use AtSign/Link.
- DEMO TRAY (MUST NOT read real metrics): sin-based oscillators over Date.now() with light random jitter, clamped `Math.min(95, Math.max(5, ...))` so values never sit stuck at 0%/100%; exact source comment `// demo values, not real metrics` appears twice (nextMetrics + tray title). Unit test proves non-stuckness: sample 3x with fake timers advanced 1s each, assert `new Set(cpuSamples).size > 1`.
- Playwright locator gotcha (re-learned): `toHaveTextContent` does NOT exist on Locator (that's jest-dom) — use `toContainText`. `getAttribute` returns a Promise, so expect.poll predicates must be `async () => (await el.getAttribute("data-active")) === "true"`.
- e2e viewport forcing for CSS-breakpoint behavior: waybar is `hidden md:flex`, so desktop assertions would FAIL in the mobile project (390px). Solution: `test.describe("waybar (desktop)")` + `test.use({ viewport: { width: 1440, height: 900 } })` and a separate mobile describe forcing 390 — every describe is deterministic in BOTH projects (22/22 total: 3 smoke + 4 wallpaper + 4 waybar x 2 projects).
- e2e reboot flow: clicking power-menu-reboot flips booted=false -> AnimatePresence swaps to BootScreen immediately; asserting `boot-screen` visible within 3s is enough (BootScreen auto-advances only after ~4s).

## Task 9 (workspace system) — 2026-08-10
- WORKSAPCES STORE CONTRACT (src/store/workspaces.ts — SINGLE source of truth for membership; wm.ts windows have NO workspace field, todo 12): constants `WORKSPACE_IDS = [1,2,3,4,5]`, `WORKSPACE_LABELS = ["term","proj","web","soc","game"]` (index 0 = workspace 1, waybar pills todo 8), `workspaceLabel(ws)`, `isWorkspaceId(ws)` guard. State: `workspaces: Record<WorkspaceId, {windows: string[], focused: string|null}>` + `activeWs`. Actions: `openInWorkspace(appId, ws?)` -> returns `win-<counter>` monotonic id (default ws = activeWs, pushes + focuses); `closeWindow(id)` removes ONLY from owning ws, closing the focused window falls focus to the window that followed it (else last remaining, else null); `moveWindow(id, ws)` removes-from-old/appends-to-new/focuses-in-new AND falls back old-ws focus when the moved window was focused — same-ws move must never duplicate; `focusNextInWs(ws)` cycles (wraps), no-op when empty; `setActive(ws)`; `getWindowWs(id)` -> WorkspaceId|null. Selectors: `selectWorkspace(ws)` (stable ref), `selectActiveWs`. NEVER persisted — no persist middleware (plain `UseBoundStore` type has no `.persist` key; tests assert it's undefined via a cast).
- CRITICAL INVARIANT for todo 12 orchestrators: a window id lives in EXACTLY ONE workspace list at all times. `lib/wm/actions.ts` `openApp(appId)` = `workspaces.openInWorkspace(appId)` THEN `wm.open({id,...})` in the same handler; `moveWindowToWorkspace(id, ws)` = `workspaces.moveWindow(id, ws)` + wm raise/bounds. The returned id string is the join key between the two stores.
- Vitest gotcha: JSX in a `.ts` test file fails to transform ("Expected `>` but found `/`" from vite:oxc) — JSX only parses in `.tsx`. A `.ts` store test that must render a component uses `render(createElement(Component))` instead. Keeps the file extension the plan specifies.
- zustand 5 `create<T>(initializer)` (non-curried) is fully typed; type guards must accept `number | undefined` when the source param is optional (`isWorkspaceId(ws: number | undefined): ws is WorkspaceId`).
- Object-literal computed keys `[owning]` + `[ws]` collide when they're equal — the LAST key wins, silently dropping the filtered windows and duplicating the moved id. Branch on `owning === ws` and reuse the filtered list (regression-tested).
- Parallel todos land side by side in the same git worktree: todo 6's page.tsx/smoke.test.tsx/boot.test.tsx were mid-edit (uncommitted, with lint/tsc errors) while todo 9 verified — full-suite gates (`tsc`, `lint`, `test:unit`) re-run after the parallel task commits; per-file vitest runs prove own-scope green.
- WorkspaceView references `window-glass` + `hairline` utilities (keeps them out of Tailwind v4 tree-shaking for todo 13).

## Task 5 (test harness + CI) — 2026-08-10

- Vitest 4 + @testing-library/react 16 + jsdom 29 + @playwright/test 1.62 installed as devDeps; npm scripts preserved verbatim (test:unit = `vitest run`, test:e2e = `playwright test`); package-lock.json is tracked so `npm ci` works in CI.
- vitest globals typing: set `globals: true` in vitest.config.ts, but write tests with explicit `import { describe, expect, it } from 'vitest'` — keeps `tsc --noEmit` green without adding "vitest/globals" to tsconfig `types`. Same for jest-dom matchers: the `@testing-library/jest-dom/vitest` import in src/test/setup.ts augments vitest's Assertion types program-wide, so `toBeInTheDocument()` type-checks in any test.
- vitest needs the `"@" -> ./src` alias configured in `resolve.alias` (Vite does NOT read tsconfig paths), and `**/e2e/**` must be in `test.exclude` or vitest tries to run Playwright specs.
- `next/image` CANNOT render under jsdom: it needs IntersectionObserver (absent in jsdom 29 — verified). The unit smoke test mocks `next/image` to a plain `<img>`, stripping the `priority`/`fill`/`sizes` props (jsx-a11y/alt-text + @next/next/no-img-element + no-unused-vars disabled locally for the double — `require()` inside the mock factory is a lint ERROR, use JSX + eslint-disable instead).
- Accessible-name gotcha: an `<img alt="Vercel logomark">` inside the "Deploy Now" link makes its accessible name "Vercel logomark Deploy Now" — match links with `/substring/` regex, not exact strings.
- playwright.config.ts: `reuseExistingServer: true` reuses a running dev server on :3000 instead of failing (used in this sandbox); webServer runs `npm run dev` otherwise. Two chromium projects: desktop 1440x900, mobile (Pixel 5 UA) 390x844.
- PLAYWRIGHT SANDBOX GOTCHA (docker, no root): `npx playwright install chromium` downloads binaries (INSTALLATION_COMPLETE present) but then FAILS host-requirement validation because the container has no browser system libs and no fonts. Do NOT "fix" playwright.config.ts for this — CI (ubuntu-latest) has everything. For local e2e runs instead export:
    export LD_LIBRARY_PATH=/tmp/opencode/pwlibs/l          # extracted .debs (glib,nss,nspr,atk,atspi,dbus,xcomposite,xdamage,xfixes,xrandr,gbm,xkbcommon,asound,drm,Xi,Xrender) from ports.ubuntu.com via apt-get -o Dir::State::lists=/tmp/opencode/aptlists download + dpkg-deb -x; iterate `ldd` until clean
    export PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS=1
    export FONTCONFIG_FILE=/tmp/opencode/fonts.conf        # minimal fontconfig pointing at extracted DejaVu Sans (fonts-dejavu-core .deb); WITHOUT fonts, glyph boxes collapse to ~0 width and Playwright reports visible text as "hidden" (empty bounding box)
  Chrome sandbox also needs `--no-sandbox` in some dockers; here plain headless shell launched fine as non-root.
- CI workflow order matters: lint -> typecheck -> test:unit -> build -> playwright. e2e must NEVER run before build. Browser cache step uses actions/cache keyed on ~/.cache/ms-playwright + package-lock hash.
- Tailwind v4 `@utility window-glass` tree-shakes when unused — it must be referenced or safelisted before a later todo uses it.

## Task 4 (fonts + icon system) — 2026-08-10
- SHARED RUNTIME DEPS (this todo was the wave's package.json owner): lucide-react ^1.31.0, zustand ^5.0.14, motion ^13.1.0 — all runtime deps (no -D). Zustand 5 is the store lib for todos 6/7/9/12; motion 13 is the animation lib for boot/window transitions. Later todos should `import { useStore } from 'zustand'` (zustand 5) and `import { motion } from 'motion/react'` (motion 13 — NOT framer-motion, the package renamed).
- next/font/google in THIS sandbox: fonts.googleapis.com IS reachable (200) — no offline fallback needed. If a future env is offline: npm registry also worked, so @fontsource tarballs (`npm pack @fontsource/inter`) would be the fallback source for woff2 -> next/font/local.
- next/font variable wiring gotcha: the @theme block in globals.css already declares --font-sans/--font-mono as fallback stacks at :root. next/font's `variable:` classes land ON the <html> element, whose class selector (0,1,1) beats :root (0,1,0), so the loaded faces win. No globals.css change was needed — body already uses var(--font-sans), and Tailwind font-mono/font-sans utilities resolve the vars at runtime. Design rule wired: terminal/waybar/launcher -> font-mono, body -> font-sans.
- Weight arrays on variable google fonts (Inter, JetBrains Mono) work fine in Next 16.3: next/font builds css2?family=X:wght@400;500;700 and downloads per-weight static instances (verified in compiled get-google-fonts-url.js).
- lucide-react 1.x REMOVED brand icons: `Github` and `Linkedin` are undefined exports (lucide dropped all brand icons in v1). Todo 8 (waybar social links) must use AtSign/Link or custom brand SVGs — do NOT import Github/Linkedin from lucide-react.
- AppIcon convention: 11 appIds render stroke-based currentColor glyphs with aria-label = humanized label ("terminal" -> "Terminal"); unknown ids -> deterministic generic app tile, never throws. AppIcon/Icon are pure SVG — safe under jsdom (no next/image, no IntersectionObserver).
- PNG generation in this sandbox: NO imagemagick/rsvg-convert/inkscape/PIL and NO `file` binary. Working method: install sharp ad-hoc in /tmp/opencode/pnggen (npm reachable), render icon.svg -> 512x512 png via sharp's librsvg input (density 300). Validate PNG by parsing signature bytes (89504e470d0a1a0a) + IHDR width/height in node — `file` doesn't exist here.

## Task 6 (boot sequence + desktop shell mount) — 2026-08-10
- SHELL MOUNT POINTS (todos 7/8/9/10 mount INTO src/app/page.tsx, which is now the ZaidOS desktop shell root; the CNA landing page is gone): the page renders `useIsHydrated()` gate -> AnimatePresence mode="wait" switching <BootScreen/> (not booted) and the desktop motion.div `data-testid="desktop"` (booted). Mount comments inside the desktop container: Wallpaper absolute inset-0 z-0, Waybar fixed top-0 inset-x-0 z-40 height var(--waybar-h), WorkspaceView absolute inset-0 z-10, DesktopIcons absolute inset-0 z-20. `booted` comes from src/store/boot.ts (zustand persist, key "zaidos-booted"); BootScreen calls `completeBoot()` -> store flip drives the AnimatePresence exit/enter, so the shell transition needs NO event plumbing between components.
- REACT 19 + react-hooks v6 (eslint-config-next 16.3) ENFORCES `react-hooks/set-state-in-effect` as an ERROR: `setMounted(true)` in an effect FAILS lint. Canonical workaround: `useIsHydrated()` via useSyncExternalStore(noopSubscribe, () => true, () => false) — server snapshot false matches SSR null output, client snapshot true re-renders after hydration, ZERO effects/setState. Same for matchMedia: useSyncExternalStore with getSnapshot guarded `typeof window.matchMedia !== 'function'` (jsdom has NO matchMedia — verified) + getServerSnapshot false. Motion's useReducedMotion would crash under jsdom for the same reason — our hook is the jsdom-safe replacement. setState inside setInterval/setTimeout callbacks is still allowed (it's the async-callback exemption).
- zustand 5 persist timing: with sync localStorage + createJSONStorage, rehydration is SYNCHRONOUS at module load (toThenable resolves plain thenables inline). So `useBootStore.getState().booted` is the persisted value on the client's first render — capture visit-start state in a lazy useState initializer, no effect needed. Server-side: `createJSONStorage(() => localStorage)` factory throws inside zustand's try/catch -> storage undefined -> hydrate skipped -> initial state. Verified SSR-safe by `next build` static generation.
- persist writes `{state: {...}, version: 0}` — unit tests assert that exact shape; per-test isolation needs `vi.resetModules()` + dynamic `await import()` because the store rehydrates at import time.
- @testing-library getByText uses getNodeText which joins ONLY DIRECT text-node children: `Zaid<span>OS</span>` does NOT match getByText(/ZaidOS/) (matches nothing) — assert "Zaid" and "OS" separately, or keep the wordmark a single text node.
- BootScreen typewriter is dependency-light (setInterval + derived char budget `charsTypedAt(elapsed)` with per-line gaps); progress bar is motion.div animate width 0->100% linear 4s; skip = window keydown + pointerdown listeners (no JSX handlers -> no jsx-a11y static-element-interaction lint); reduced-motion path renders full content + full bar on first paint and auto-advances after 800ms hold (REDUCED_HOLD_MS) — e2e asserts desktop appears without interaction.

## Task 12 (wm store + hotkey service) — 2026-08-10
- WM STORE CONTRACT (src/store/wm.ts, session-only, no persist — tests assert `.persist` undefined via cast, workspaces pattern): state `windows: Record<string, WindowState>` + `nextZ`. Window = `{ id, appId, title, x, y, w, h, z, minimized, maximized, mode: "tile"|"float" }` — NO `workspace` field (membership ONLY in workspaces.ts). Actions `open(input)` (z = nextZ+1, opens floating, centered below waybar, clamps defaultSize), `close(id)` (removes; if the closed window had z === nextZ it promotes the next-highest-z window in the SAME workspace to nextZ+1 so the top-z invariant "top window has z === nextZ" holds), `focus(id)` (refuses when `getWindowWs(id) !== activeWs` — cross-workspace raising forbidden; un-minimizes + raises; if already top it only restores visibility, so focus twice NEVER grows z unbounded), `minimize(id)` (idempotent set true — restore goes through focus()), `toggleMaximize(id)`, `setBounds(id, {x?,y?,w?,h?})`, `setMode(id, mode)`, `reclampToViewport()` (for the future ResizeObserver). Pure helpers in wm.ts: `clampWindowBounds({w,h}, {vw,vh})` (re-exported by actions.ts) and `getViewport()` (SSR fallback 1280x800).
- CLAMP MATH (exact, viewport-capped): `w = clamp(w, Math.min(360, vw-16), vw-16)`, `h = clamp(h, Math.min(240, vh-WAYBAR_H-16), vh-WAYBAR_H-16)` with WAYBAR_H=40 (--waybar-h token). On a 200x200 viewport the MINIMUM caps too: w=184, h=144 (NOT 360x240) — the acceptance test drives exactly this. clamps applied in setBounds, open (defaultSize 640x480), and reclampToViewport.
- ORCHESTRATOR ORDER MATTERS for close: `closeWindow(id)` must call `wm.close(id)` BEFORE `workspaces.closeWindow(id)` — wm.close reads the closed window's owning workspace from the workspaces store to promote the next-highest, and workspaces removes the id. openApp/moveWindowToWorkspace/closeWindow are the ONLY sanctioned entry points; both stores mutate in one synchronous handler (React 19 batches into one commit). moveWindowToWorkspace raises only when the target ws === activeWs (window visible) — never cross-workspace; it also re-clamps bounds defensively.
- HOTKEYS (src/lib/hotkeys.ts, standalone — no store imports, handlers are injected): `resolveHotkey(event, platform?)` is pure + testable (optional platform overrides detectPlatform() so tests are deterministic on any host). Mod = Meta on Mac, Ctrl+Alt elsewhere — modActive checks `meta && !ctrl && !alt` (mac) vs `ctrl && alt && !meta` (other). Key map keyed by `event.key`, lowercased ONLY for single-char keys ("ArrowLeft"/"Tab"/"Enter" must not be lowercased). Ignore rules live INSIDE resolveHotkey: open modal (`setModalOpen(bool)`) and editable targets. `initHotkeys(handlers, platform?)` attaches ONE window keydown listener guarded by a module-level `activeListener` — a second call is a no-op (HMR-safe); `disposeHotkeys()` removes it; matched hotkeys `preventDefault()` (needs `cancelable: true` on constructed KeyboardEvents in tests or defaultPrevented stays false).
- JSDOM 29 QUIRK (verified by probe test): `HTMLElement.isContentEditable` is UNDEFINED (not implemented) and the `contenteditable` ATTRIBUTE is not reflected — only the IDL property works (`el.contentEditable = "true"` reads back "true"). isEditableTarget must check `target.isContentEditable || target.contentEditable === "true"` to be correct in BOTH jsdom and real browsers.
- zustand cross-store reads: calling `useWorkspacesStore.getState()` INSIDE another store's `set()` updater is fine (synchronous); `set` returning the same state object `s` skips the notification (workspaces pattern — used for no-op guards like "already top").
- Test isolation: reset BOTH stores in beforeEach — `useWmStore.setState({ windows: {}, nextZ: 0 })` + `useWorkspacesStore.setState({ workspaces: createInitialWorkspaces(), activeWs: 1 })` (setState merges; actions survive). Mock the client viewport for clamp tests via `Object.defineProperty(window, "innerWidth", { value, configurable: true })` + innerHeight (jsdom defaults 1024x768; restore in afterEach). Never mock nextZ by hand — openApp gives real monotonic ids from the shared counter.
- Parallel-task note: task 7's `e2e/wallpaper.spec.ts` was modified mid-flight in the same worktree; task 12 gates ran clean anyway (my scope untouched).

## Task 7 (wallpaper engine) — 2026-08-10
- WALLPAPER STORE CONTRACT (src/store/wallpaper.ts — zustand persist, key "zaidos-wallpaper", SSR-safe via createJSONStorage(() => localStorage) same as boot.ts): state `type: WallpaperType` (union "matrix"|"gradient"|"dark"|"light"), `setWallpaper(type)`, selector `selectWallpaperType`. partialize -> `{type}` only; custom `merge` guard coerces corrupted/unknown persisted values back to the default (matrix) instead of leaving the layer empty (regression-tested with a seeded `{state:{type:"neon"}}`). Tests assert persisted shape `{state:{type:...}, version:0}` — vi.resetModules + dynamic import pattern from boot.test.tsx.
- DEV-ONLY DEBUG HANDLE: `window.__zaidosWallpaper.setWallpaper(type)` attached in the store module only when `process.env.NODE_ENV === "development"` (Next inlines it) + `typeof window !== "undefined"` (SSR-safe). This is how the e2e drives the store before the real picker exists (todo 10 context menu); the e2e also seeds localStorage via addInitScript to test the full persist->rehydrate->render path without reloading state.
- MATRIX RAIN (MatrixRain.tsx — ORIGINAL column-model renderer, NOT the canonical snippet): each drop owns a pre-generated glyph trail (katakana+digits) + head y; per drawn frame it unshifts a fresh glyph, fades trail cells accent-green 100%->15%, draws the head in the LIGHT TEXT token for contrast, and applies a translucent 8.5% background wash for the motion smear. Canvas colors are read ONCE at mount from getComputedStyle(--color-zaid-accent/--color-zaid-bg/--color-zaid-text/--font-mono) with fallbacks mirroring the token hex values (jsdom has no custom props -> fallbacks keep the renderer alive).
- PERF GUARDS: rAF loop capped ~30fps via frame-skip (FRAME_MS=33, only draws when `t - last >= FRAME_MS`); visibilitychange CANCELS the rAF chain while hidden and restarts on visible; devicePixelRatio capped at 2x; backing store only resized when clientWidth/Height actually change (checked per frame inside fit()).
- REDUCED MOTION: paints exactly ONE static frame (opaque bg + 3 mid-brightness glyphs per column), sets `canvas.dataset.frames = "1"`, and NEVER starts the rAF loop. `data-frames` is the observable "no loop" proof: e2e reads it, waits 600ms, reads again — must stay "1" (also asserted in unit test via matchMedia stub — jsdom 0-size canvas means fit() bails, so the counter is set unconditionally after drawStatic).
- WALLPAPER WRAPPER (Wallpaper.tsx): `[data-testid="wallpaper"]` + `data-theme={type}` + aria-hidden, renders 4 variants behind an AnimatePresence keyed motion.div (key={type}) — crossfade 0.6s, duration 0 under reduced motion, initial={false} so first mount doesn't double-fade with the desktop. Gradient = motion animating `filter: hue-rotate(360deg)` 24s linear infinite on a token gradient (GPU-composited per the animation axiom; static under reduced motion). dark/light = deterministic abstract inline SVG (no RNG at render — SSR-safe): token-driven blobs (radialGradient), concentric rings, dot-grid horizon with arithmetic skip pattern, diagonal hairline. light bg uses `color-mix(in srgb, var(--color-zaid-text) 90%, white)` — no hardcoded hex.
- E2E GOTCHA (bit me twice): `locator.getAttribute()` returns a PROMISE — inside `expect.poll`, `Number(canvas.getAttribute("data-frames") ?? "0")` computes `Number(Promise)` = NaN (poll receives NaN forever). The predicate MUST be async: `Number((await canvas.getAttribute(...)) ?? "0")`. The green-pixel scan also needs to be an expect.poll (single evaluate is flaky if it lands mid-wash/compile).
- E2E ENV NOTES: cold dev-server Turbopack compiles can blank the page mid-test (error-context snapshots show an empty page) — the wallpaper poll has a 3s window + the pixel scan retries up to 5s, which absorbed it. The "alert"/"nextjs-portal" elements in Playwright page snapshots are Next 16's DEV TOOLS floating widget, NOT an error overlay — don't misread them as app failures.
- jsdom canvas: `getContext("2d")` returns null and logs "Not implemented" — MatrixRain early-returns (no crash, no loop). Unit tests stub `HTMLCanvasElement.prototype.getContext` with a Proxy that answers any method call with a no-op and accepts any property set, so the wrapper test exercises the real effect lifecycle. The reduced-motion wrapper test renders via `render(createElement(Wallpaper))` in a .ts file (JSX fails to transform in .ts — learned in task 9).
- Mounted at z-0 inside the desktop motion.div in page.tsx, replacing the bg-zaid-bg placeholder div; the TODO(todo 7) mount comment and all other mount comments (8/9/10) are untouched. Parallel task 12's wm store landed mid-work: its uncommitted wm.test.ts broke the full test:unit suite until they committed — own-scope vitest runs (wallpaper/boot/smoke) proved green the whole time.
