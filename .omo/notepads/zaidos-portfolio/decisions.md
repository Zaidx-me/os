## 2026-08-10 Orchestrator scheduling decisions (wave 1-3)
- T4 (fonts/icons) dispatched FIRST with extended scope: also installs `zustand` + `motion` (shared runtime deps needed by todos 6/7/9/12). package.json is single-owner per wave; T4 is the designated installer so later waves never touch it.
- Dispatch order T4 -> T6 -> {T7,T9} -> {T8,T12} -> {T11,T13} -> T14,T15 -> T10,T16. Rationale: T6 owns the shell mount point (page.tsx/DesktopShell); T7 wallpaper mounts into it; T9 workspaces store is needed by T8 (pills) and T12 (actions.ts imports workspaces.ts); T12 unblocks T11/T13/T14; T10's acceptance (double-click opens window) needs wm store + app registry (T12/T15), so despite plan "Blocked by: 2", T10 dispatches after T15. T8 acceptance (click pill switches workspace) needs T9's store, so despite plan "Blocked by: 2,6", T8 dispatches after T9.
- REVISED wave-2/3 schedule (2026-08-10, after T7+T12 verified): page.tsx is single-owner per round — T7 (done), then T8 (Waybar mount), then T13 (Window + WorkspaceView mount — T13's Playwright acceptance needs a window rendered, and WorkspaceView is NOT mounted yet; T9 only created the component), then T11 (Launcher + WorkspaceView is already mounted by T13 — T11's "Enter opens terminal window" acceptance also needs WorkspaceView mounted). So page.tsx rounds: T8 -> T13 -> T11. After T13: {T14 (tiling, touches Window.tsx only) + T15 (app registry, lib-only)} can pair with T11. Then {T10 (DesktopIcons, page.tsx z-20 mount) + T16 (waybar taskbar — Waybar.tsx file, NOT page.tsx)} pair last.
- HOTKEY SHELL WIRING deferred to T11 (2026-08-10): hotkeys.ts (todo 12) is committed with injected handlers, but `initHotkeys` attaches exactly ONE window listener (module guard — a second call is a no-op). If T8 wired a partial handler set, T11 could NOT extend it without dispose+re-init. T11 wires the FULL handler map (its Mod+Space acceptance REQUIRES the shell mount) including selectWorkspace -> workspaces.setActive (Mod+1..5 live from T11 onward). T8 does NOT call initHotkeys.
- Waybar launcher button bridge (2026-08-10): T8 renders the launcher button (testid waybar-launcher) with a click handler that dispatches a window CustomEvent `zaidos:toggle-launcher`; T11's Launcher mounts a listener for it (code comment TODO(todo 11) marks the contract). No shared store needed.
- Power menu Reboot (2026-08-10): T8 adds ONE action to src/store/boot.ts — `resetBoot: () => set({ booted: false })`. Flipping booted=false makes page.tsx's AnimatePresence swap desktop -> BootScreen (boot replays; completeBoot() re-boots). Note: after a reboot completes, the returning-visitor quick-fade overlay (boot-flash, wasBootedAtMount captured true at mount) will briefly show — cosmetic, acceptable.
- T13 dispatch (2026-08-10, next page.tsx owner): page.tsx rounds are T8 (done) -> T13 (Window chrome + mount <WorkspaceView/> replacing the TODO(todo 9) comment) -> T11 (launcher + initHotkeys wiring). T13 is dispatched ALONE (T14 touches Window.tsx, T11 touches page.tsx, T15 blocked by 13).
- T13 acceptance "minimize hides (aria-hidden) + task appears": the waybar TASKBAR is todo 16's component and Waybar.tsx is T8's committed file — T13 MUST NOT modify Waybar.tsx. T13 e2e asserts minimize -> aria-hidden + window hidden; the "task appears" assertion lands in T16's acceptance.
- T13 e2e needs windows openable BEFORE desktop icons (T10)/launcher (T11)/hotkey wiring exist: dev-only debug handle `window.__zaidosOpenApp(appId)` added ADDITIVELY to src/lib/wm/actions.ts (NODE_ENV==="development" + typeof window guard — mirrors the __zaidosWallpaper precedent in wallpaper.ts). Must not alter openApp/closeWindow/moveWindowToWorkspace signatures.
- T13 edge-zone seam: Window.tsx implements the 40px edge detection + glass snap-preview overlay (left/right/full by pointer x) but NO snap-on-drop — T14 owns the drop math (x=8, y=40+8, w=50%-12px). Seam (preview state + a documented hook area) left for T14.
- T13 "drag while maximized": restore-first per plan line 196 (unmaximize then drag). QA "failure: maximize then drag -> no position change" guards the ANTI-behavior — the test asserts drag after maximize DOES move (restore-first then drag).
- T13 Window root must expose `data-testid="window-{appId}"` + `data-window-id` — T15's acceptance ("[data-testid="window-<appId>"]") depends on this contract. WorkspaceView keeps data-testid="workspace-view" + the ws-empty-hint; T13 swaps the placeholder tiles for real Window components.
